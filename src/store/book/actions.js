import { actions } from './reducer';

export const {
  added: bookAdded,
  removing: removingBook,
  removed: bookRemoved,
  edited: bookEdited,
  commentAdded,
  commentRemoved,
} = actions;